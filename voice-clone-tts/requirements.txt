# Voice Clone TTS - 依赖列表
# v3.2 微服务架构

# ===================== 核心框架 =====================
fastapi>=0.104.0        # 统一版本：与 pyproject.toml 保持一致
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
httpx>=0.25.0
websockets>=12.0

# ===================== 深度学习 =====================
torch>=2.0.0
torchaudio>=2.0.0

# ===================== TTS 引擎 =====================
# Coqui TTS (XTTS-v2)
TTS>=0.22.0

# OpenVoice 依赖 (需从源码安装)
# git clone https://github.com/myshell-ai/OpenVoice.git packages/repos/OpenVoice
# pip install -e packages/repos/OpenVoice

# MeloTTS (OpenVoice 基础 TTS)
edge-tts>=6.1.0

# ===================== 音频处理 =====================
numpy>=1.24.0           # 统一版本：与 pyproject.toml 保持一致
scipy>=1.10.0           # 统一版本：与 pyproject.toml 保持一致
librosa>=0.10.0
soundfile>=0.12.0

# ===================== 工具 =====================
psutil>=5.9.0           # 系统监控
python-multipart>=0.0.6 # 文件上传

# ===================== 可选依赖 =====================
# transformers<4.50      # 某些模型需要特定版本
# ctranslate2==4.4.0     # Whisper 加速
# pyannote.audio>=3.1.0  # VAD (需要 HuggingFace token)
# speechbrain>=1.0.0     # 说话人嵌入 (可选)
